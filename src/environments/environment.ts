// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCN_noSts1nu_tLLfheByUpTfmwFhV0Bp4",
    authDomain: "businesscards-4e744.firebaseapp.com",
    databaseURL: "https://businesscards-4e744.firebaseio.com",
    projectId: "businesscards-4e744",
    storageBucket: "businesscards-4e744.appspot.com",
    messagingSenderId: "952718174883",
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
